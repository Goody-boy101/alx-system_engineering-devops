#!/bin/bash

# Check if the current owner of hello is guillaume
if [[ $(stat -c '%U' hello) == "guillaume" ]]; then
    # Change the owner of hello to betty
    chown betty hello
    echo "Ownership of hello changed to betty."
else
    echo "hello is not owned by guillaume. No changes made."
fi
